enum UserRoleEnum {
  subscriber,
  contributor,
  author,
  editor,
  administrator,
}

enum MediaTypeEnum {
  image,
  video,
  text,
  application,
  audio,
}

enum MimeTypeEnum {
  image,
  video,
  file,
}

enum PostStatusEnum {
  publish,
  future,
  draft,
  pending,
  private,
  trash,
}

enum CategoryOrderByEnum {
  name,
  slug,
  description,
  count,
}
