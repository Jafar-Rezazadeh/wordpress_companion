enum UserRole {
  subscriber,
  contributor,
  author,
  editor,
  administrator,
}

enum MediaType {
  image,
  video,
  text,
  application,
  audio,
}

enum MimeType {
  image,
  video,
  file,
}

enum PostStatus {
  publish,
  future,
  draft,
  pending,
  private,
  trash,
}
